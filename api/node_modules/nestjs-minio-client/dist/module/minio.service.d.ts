import { OPTIONS_TYPE } from '../utils';
import type { MinioClient, MinioCopyConditions } from '../utils';
export declare class MinioService {
    private options;
    private readonly minioSdk;
    private readonly copyConditionsImplementation;
    constructor(options: typeof OPTIONS_TYPE);
    get client(): MinioClient;
    get copyConditions(): MinioCopyConditions;
}
