import { OPTIONS_TYPE } from './minio.module-definition';
import type { MinioClient, MinioCopyConditions } from './minio.interface';
export declare function createMinioClient(options: typeof OPTIONS_TYPE): MinioClient;
export declare function createCopyConditions(): MinioCopyConditions;
